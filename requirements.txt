# Data
yfinance>=0.2.40
pandas>=2.0
numpy>=1.24
scipy>=1.10
requests>=2.31

# Backend
# (SQLite is stdlib — no dependency)

# Phase 2 (live backend) — commented until needed
# fastapi>=0.110
# uvicorn>=0.29

# Testing
pytest>=8.0
